name: quarters-frontend
services:
  - name: quarters-frontend
    github:
      repo: https://github.com/MaitisamY/quarters-frontend.git  # Replace with your GitHub repository
      branch: main  # Replace with your branch name
    build_command: npm run build
    envs:
      - key: NODE_ENV
        value: production