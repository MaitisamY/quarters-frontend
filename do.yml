name: quarters-frontend
region: toronto
services:
  - name: quarters-frontend
    github:
      repo: https://github.com/MaitisamY/quarters-frontend.git
      branch: main
    build_command: npm run build
    run_command: npm install -g serve && serve -s build

        